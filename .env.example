# Archivo de ejemplo de variables de entorno para Backend Restaurant
# 
# INSTRUCCIONES:
# 1. Copia este archivo a .env:  cp .env.example .env
# 2. Edita .env con tus valores reales
# 3. NUNCA commites el archivo .env a git (ya está en .gitignore)

# ============================================
# MySQL Database Configuration
# ============================================
MYSQLHOST=localhost
MYSQLUSER=tu_usuario_mysql
MYSQLPASSWORD=tu_contraseña_segura_aquí
MYSQLDATABASE=restaurant_db
MYSQLPORT=3306

# ============================================
# Server Configuration
# ============================================
PORT=3001
NODE_ENV=production

# ============================================
# CORS Configuration
# ============================================
# URL del frontend para permitir peticiones CORS
# En desarrollo local: http://localhost:5173
# En producción: http://tu-dominio.com o http://tu-ip-servidor
FRONTEND_URL=http://192.168.0.12

# ============================================
# Notas de Seguridad
# ============================================
# - Usa contraseñas fuertes para MySQL
# - En producción, considera usar HTTPS (https://)
# - Limita acceso a MySQL solo desde localhost si backend y DB están en el mismo servidor
# - Mantén este archivo fuera de git (.gitignore)
# - En servidores compartidos, usa permisos restrictivos: chmod 600 .env
